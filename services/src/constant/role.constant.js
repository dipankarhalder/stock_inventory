const userRole = {
  SUPER: 'super_admin',
  ADMIN: 'admin',
  STUFF: 'staff',
};

const taxesTypes = {
  exclusive: 'exclusive',
  inclusive: 'inclusive',
};

const taxesStatus = {
  active: 'active',
  inactive: 'inactive',
};

module.exports = {
  userRole,
  taxesTypes,
  taxesStatus,
};
